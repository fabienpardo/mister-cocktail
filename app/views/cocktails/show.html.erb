<div class="fullpage">
  <div class="content text-center">
    <h1 class="title_page"><%= @cocktail.title %></h1>
    <p class="content_page"><%= @cocktail.content %></p>

    <%= link_to 'Dose-moi !', new_cocktail_dose_path(@cocktail), class: 'link_page btn btn-warning' %>
  </div>
</div>
